---
interface Props {
  url: string;
  newTab?: boolean;
}

const { url, newTab } = Astro.props;
---

<a
  href={url}
  class="text-blue-600 hover:underline"
  target={newTab ? "_blank" : "_self"}><slot /></a
>
